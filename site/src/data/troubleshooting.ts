import type { FilterRow } from "../components/FilterTable.astro";

/**
 * Symptoms and their causes, drawn only from behaviour that exists in this
 * repository. Every exact string here appears in the source.
 */
export const TROUBLESHOOTING: FilterRow[] = [
  {
    key: "TANDEM_NAMESPACE does not match the configured INIT tuple",
    label: "Boot failure",
    meta: "config",
    tags: ["config", "boot"],
    body: "The namespace you supplied differs from the one derived from your network, INIT txid, and spec hash. Almost always a typo in the txid or the spec hash rather than a wrong namespace.",
    note: "Recompute it with the configuration builder. Remember the INIT txid is supplied in display order and reversed internally, while the spec hash is a raw digest and is never reversed.",
  },
  {
    key: "TANDEM_CLOSE_HEIGHT must equal open height plus 4320",
    label: "Boot failure",
    meta: "config",
    tags: ["config", "boot"],
    body: "The founding window is a fixed 4,320 blocks, so the close height is derived from the open height rather than chosen independently.",
    note: "Set close height to open height plus 4320 exactly.",
  },
  {
    key: "TANDEM_INIT_HEIGHT must precede the open height by at least 1008 blocks",
    label: "Boot failure",
    meta: "config",
    tags: ["config", "boot"],
    body: "The INIT lead rule requires at least 1,008 blocks between the INIT confirming and the first height at which a CREATE can be valid.",
    note: "Either lower the init height or raise the open height, and remember the close height moves with it.",
  },
  {
    key: "<NAME> is required",
    label: "Boot failure",
    meta: "config",
    tags: ["config", "boot"],
    body: "One of the fourteen required variables is missing or empty. Values are trimmed, so a variable set to only whitespace counts as missing.",
    note: "The message names the exact variable. Check the environment reference for its format.",
  },
  {
    key: "<NAME> must be 32-byte lowercase hex",
    label: "Boot failure",
    meta: "config",
    tags: ["config", "boot"],
    body: "A hash-shaped variable is not 64 lowercase hexadecimal characters. Uppercase hex is rejected even though the bytes would be identical.",
    note: "Applies to the INIT txid, the spec hash, the namespace, and the two binary digests.",
  },
  {
    key: "<NAME> must be valid JSON",
    label: "Boot failure",
    meta: "config",
    tags: ["config", "boot", "verification"],
    body: "A trusted key map is not parseable. In a shell or compose file the braces and quotes often need escaping.",
    note: "The empty value is the two characters {} and that is a valid, closed configuration.",
  },
  {
    key: "<NAME> must be an HTTP URL without credentials, query, or fragment",
    label: "Boot failure",
    meta: "config",
    tags: ["config", "boot", "verification"],
    body: "The pipeline B base URL carries a username, password, query string, or fragment, or uses a scheme other than http or https.",
    note: "Supply the origin and path prefix only. Trailing slashes are stripped for you.",
  },
  {
    key: "AGREEMENT_KEY_ID is required when an agreement key is configured",
    label: "Boot failure",
    meta: "config",
    tags: ["config", "boot", "signing"],
    body: "A private or public agreement key was supplied with no key id. A verifier selects the trusted public key by key id, so a key without one cannot be used.",
  },
  {
    key: "canonical_tip_missing",
    label: "Readiness stays false",
    meta: "readiness",
    tags: ["readiness"],
    body: "No row exists in the canonical block table, so there is no tip to report.",
    note: "Expected with only this service running. Nothing in the repository ingests blocks or writes a canonical row, so a deployment has to supply that driver.",
  },
  {
    key: "checkpoint_incomplete",
    label: "Readiness stays false",
    meta: "readiness",
    tags: ["readiness"],
    body: "There is no checkpoint at exactly the canonical height. The gate requires equality, not merely a checkpoint somewhere behind the tip.",
    note: "Also expected on a fresh install. No code path inserts a checkpoint row, so populating that table is an operator responsibility.",
  },
  {
    key: "agreement_signer_unavailable",
    label: "Readiness stays false",
    meta: "readiness",
    tags: ["readiness", "signing"],
    body: "The signing boundary is incomplete. It needs a key id, a 32-byte private key, and all four release identity values before it counts as configured.",
    note: "If a public key is also supplied it must equal the one derived from the private key, otherwise the whole boundary is treated as unconfigured with no separate error.",
  },
  {
    key: "bitcoin_network_mismatch",
    label: "Readiness stays false",
    meta: "readiness",
    tags: ["readiness", "bitcoin"],
    body: "The chain reported by getblockchaininfo does not equal the value derived from your configured network. The comparison is exact.",
    note: "The mainnet network maps to the chain name main, not mainnet. Every other network uses its own name.",
  },
  {
    key: "bitcoin_core_unavailable",
    label: "Readiness stays false",
    meta: "readiness",
    tags: ["readiness", "bitcoin"],
    body: "The RPC call failed or timed out. There is exactly one attempt with no retry or backoff, so a transient failure shows immediately.",
    note: "Inside a container a loopback RPC URL points at the container itself. Use a reachable host address.",
  },
  {
    key: "bitcoin_core_initial_block_download",
    label: "Readiness stays false",
    meta: "readiness",
    tags: ["readiness", "bitcoin"],
    body: "The node is still performing initial block download and its view of the chain is not final.",
    note: "Wait. Indexing a node that is still syncing would produce state you would have to discard.",
  },
  {
    key: "canonical_tip_stale",
    label: "Readiness stays false",
    meta: "readiness",
    tags: ["readiness"],
    body: "The node height exceeds the canonical height by more than the configured lag, which defaults to 2 blocks.",
    note: "Raising the lag hides the symptom rather than fixing it. Look at why indexing is behind.",
  },
  {
    key: "database_unavailable",
    label: "Readiness stays false",
    meta: "readiness",
    tags: ["readiness", "database"],
    body: "The probe's SELECT 1 failed. Any database error is collapsed into this single reason, so the detail is in the service log rather than in the response.",
  },
  {
    key: "verification_unavailable",
    label: "Every verified route returns 503",
    meta: "verification",
    tags: ["verification", "api"],
    body: "Agreement did not hold. The body is identical for every cause, deliberately, so a caller cannot probe which check failed.",
    note: "The specific reason is in the service log as a warning from the gateway. Check readiness first, then the pipeline B endpoint, then the trust maps, then the compared fields.",
  },
  {
    key: "pipeline B endpoint is not configured",
    label: "Verified routes closed",
    meta: "verification",
    tags: ["verification"],
    body: "PIPELINE_B_BASE_URL is unset, so there is no second opinion to request. This is the default state.",
  },
  {
    key: "pipeline B key is not trusted",
    label: "Verified routes closed",
    meta: "verification",
    tags: ["verification", "signing"],
    body: "The envelope's key id is absent from the configured trust map for that pipeline. The signature is never checked, because there is no key selected to check it with.",
    note: "During a rotation, add the new key id before the other pipeline starts using it.",
  },
  {
    key: "agreement mismatch at <field>",
    label: "Verified routes closed",
    meta: "verification",
    tags: ["verification"],
    body: "One of the nine compared fields differs between the two pipelines. The message names the first field that differed, in the order the code checks them.",
    note: "A mismatch at height usually means one pipeline is behind. A mismatch at block_hash means they are on different chains. A mismatch at a root means a genuine implementation difference and should be investigated rather than routed around.",
  },
  {
    key: "verified agreement changed during data read",
    label: "Verified routes closed",
    meta: "verification",
    tags: ["verification"],
    body: "Agreement held before the query and differed after it. The gateway verifies twice around every read and discards the response if anything moved.",
    note: "Occasional occurrences near a chain reorganization are normal. Sustained occurrences suggest an unstable tip.",
  },
  {
    key: "verified mainnet responses are disabled",
    label: "Verified routes closed",
    meta: "verification",
    tags: ["verification", "mainnet"],
    body: "The network is mainnet and TANDEM_VERIFIED_MAINNET_ENABLED is not exactly the string true. The gateway refuses before it asks this pipeline to sign anything.",
    note: "This is a deliberate gate and not a bug. Read the mainnet gates page before changing it.",
  },
  {
    key: "pipeline B response is too large",
    label: "Verified routes closed",
    meta: "verification",
    tags: ["verification"],
    body: "The second pipeline returned more than 65,536 bytes. The limit is enforced both on the declared content length and while streaming, so a lying header does not get through.",
    note: "An agreement envelope is small. A response near that size usually means the endpoint returned something other than an envelope, such as an HTML error page.",
  },
  {
    key: "Container is healthy but traffic fails",
    label: "Operational surprise",
    meta: "operations",
    tags: ["operations", "readiness"],
    body: "The image health check probes liveness only. It reports healthy as long as the process answers, regardless of whether any dependency is available.",
    note: "Gate traffic on readiness instead. Do not interpret container health as agreement availability.",
  },
  {
    key: "Prometheus gauges look stale",
    label: "Operational surprise",
    meta: "operations",
    tags: ["operations", "monitoring"],
    body: "The ready and canonical height gauges are updated as a side effect of a request to the readiness endpoint rather than on a timer.",
    note: "Scrape readiness as well as metrics, or the values will only move when something else happens to ask.",
  },
  {
    key: "HTTP 500 from the agreement endpoint",
    label: "Operational surprise",
    meta: "operations",
    tags: ["operations", "signing"],
    body: "A checkpoint exists at the requested height but the signing boundary is not configured, so the service cannot produce an envelope.",
    note: "Configure the key id, private key, and all four release identity values.",
  },
  {
    key: "ZMQ is configured but nothing happens",
    label: "Operational surprise",
    meta: "operations",
    tags: ["operations", "bitcoin"],
    body: "The service subscribes to the configured topics and emits internal events, and no listener is registered for any of them, so notifications are received and discarded.",
    note: "Treat ZMQ as intended design rather than working delivery. It does not currently trigger polling, because there is no polling loop to trigger.",
  },
  {
    key: "Address lookup returns nothing",
    label: "API surprise",
    meta: "api",
    tags: ["api"],
    body: "Carrier addresses are matched against a database-generated column computed from the stored keys, so a lookup only finds carriers the indexer has actually recorded.",
    note: "Also check the prefix. The address must use the human readable part for the deployment network, and a mainnet address is rejected outright on a signet deployment.",
  },
  {
    key: "limit must be between 1 and 200",
    label: "API surprise",
    meta: "api",
    tags: ["api"],
    body: "Every list route caps its row count. There are no cursors, offsets, page tokens, or total counts anywhere in the API.",
  },
];
