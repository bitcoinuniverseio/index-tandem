import type { FilterRow } from "../components/FilterTable.astro";

/**
 * Every environment variable the service reads, taken from
 * `src/config/configuration.ts` and `.env.example`.
 *
 * `meta` is the group used for faceting. `note` carries the thing that is easy
 * to get wrong.
 */
export const ENV_GROUPS = [
  "binding",
  "service",
  "bitcoin",
  "database",
  "readiness",
  "signing",
  "verification",
  "compose",
] as const;

export const ENV_VARS: FilterRow[] = [
  {
    key: "TANDEM_NETWORK",
    label: "Required. One of mainnet, signet, testnet4, regtest.",
    meta: "binding",
    tags: ["binding", "required"],
    body: "Selects the network byte used in every marker and in the namespace commitment, and sets the chain identity the readiness probe expects from Bitcoin Core. The value mainnet maps to the chain name main when comparing against getblockchaininfo.",
    note: "An unrecognised value fails the boot with `unsupported TANDEM_NETWORK`.",
  },
  {
    key: "TANDEM_INIT_TXID",
    label: "Required. 32-byte lowercase hex.",
    meta: "binding",
    tags: ["binding", "required"],
    body: "The configured INIT transaction id in the usual display order. It is one third of the deployment binding and it is never discovered by scanning. A different txid is a different protocol identifier even when every payload field is identical.",
    note: "Reversed to wire order exactly once before it enters the namespace preimage.",
  },
  {
    key: "TANDEM_SPEC_HASH",
    label: "Required. 32-byte lowercase hex.",
    meta: "binding",
    tags: ["binding", "required"],
    body: "SHA256 over the exact bytes of the normative specification file. It is the third part of the binding, and the INIT payload has to carry the same value for the deployment to activate.",
    note: "A raw digest, so unlike a txid it is never byte reversed.",
  },
  {
    key: "TANDEM_INIT_HEIGHT",
    label: "Required. Non-negative integer.",
    meta: "binding",
    tags: ["binding", "required"],
    body: "The canonical height the configured INIT transaction confirms at. It also sets the floor for reorg rollback, which never crosses one block below it.",
    note: "The open height must be at least 1,008 blocks later or the boot fails.",
  },
  {
    key: "TANDEM_OPEN_HEIGHT",
    label: "Required. Non-negative integer.",
    meta: "binding",
    tags: ["binding", "required"],
    body: "The first height at which a CREATE can be valid. A CREATE confirming before it is rejected with BAD_HEIGHT_OR_PHASE.",
  },
  {
    key: "TANDEM_CLOSE_HEIGHT",
    label: "Required. Must equal the open height plus 4,320.",
    meta: "binding",
    tags: ["binding", "required"],
    body: "The end of the founding window, as a half open bound. A CREATE at or after it still creates a valid object, just not a founding one.",
    note: "Any other value fails the boot with a message naming the 4,320 block window.",
  },
  {
    key: "TANDEM_NAMESPACE",
    label: "Required. 32-byte lowercase hex.",
    meta: "binding",
    tags: ["binding", "required"],
    body: "The namespace commitment over the domain tag, the network byte, the INIT txid in wire order, and the spec hash. Every non-INIT marker has to carry this exact value or it is rejected as WRONG_NAMESPACE.",
    note: "The service derives it independently and refuses to start if your value differs, so it is a checksum on the whole binding rather than a free parameter.",
  },
  {
    key: "NODE_ENV",
    label: "Optional. Defaults to development.",
    meta: "service",
    tags: ["service"],
    body: "Reported in the service configuration. Compose sets it to production.",
  },
  {
    key: "HTTP_HOST",
    label: "Optional. Defaults to 127.0.0.1.",
    meta: "service",
    tags: ["service"],
    body: "The interface the HTTP server binds to. Compose overrides it to 0.0.0.0 so the published port reaches the application.",
    note: "Set this explicitly only when you intend to accept connections from outside the host. There is no authentication in front of any route.",
  },
  {
    key: "PORT",
    label: "Optional. Defaults to 3021.",
    meta: "service",
    tags: ["service"],
    body: "The listening port. The container health check reads the same variable when probing liveness.",
  },
  {
    key: "BITCOIN_RPC_URL",
    label: "Required.",
    meta: "bitcoin",
    tags: ["bitcoin", "required"],
    body: "The JSON-RPC endpoint of a dedicated Bitcoin Core node. Inside a container this must not be a loopback address, because loopback points at the container itself.",
  },
  {
    key: "BITCOIN_RPC_USER",
    label: "Required.",
    meta: "bitcoin",
    tags: ["bitcoin", "required"],
    body: "The RPC user, sent with HTTP Basic authentication on every call.",
  },
  {
    key: "BITCOIN_RPC_PASSWORD",
    label: "Required.",
    meta: "bitcoin",
    tags: ["bitcoin", "required", "secret"],
    body: "The RPC password. Load it from a secret manager rather than a file in source control.",
  },
  {
    key: "BITCOIN_RPC_TIMEOUT_MS",
    label: "Optional. Defaults to 15000.",
    meta: "bitcoin",
    tags: ["bitcoin"],
    body: "Abort timeout for a single RPC call. There is one attempt and no retry, backoff, or connection pooling, so this value is the entire patience budget.",
  },
  {
    key: "BITCOIN_ZMQ_HASHBLOCK",
    label: "Optional. Blank by default.",
    meta: "bitcoin",
    tags: ["bitcoin"],
    body: "A ZMQ endpoint for block hash notifications. When set, the service subscribes and emits an internal event.",
    note: "Nothing currently listens for that event, so notifications are received and discarded. Treat ZMQ as intended design rather than as working delivery.",
  },
  {
    key: "BITCOIN_ZMQ_RAWTX",
    label: "Optional. Blank by default.",
    meta: "bitcoin",
    tags: ["bitcoin"],
    body: "A ZMQ endpoint for raw transaction notifications.",
    note: "As with the block topic, no listener is registered. There is also no raw transaction deserializer in this repository.",
  },
  {
    key: "BITCOIN_ZMQ_SEQUENCE",
    label: "Optional. Blank by default.",
    meta: "bitcoin",
    tags: ["bitcoin"],
    body: "A ZMQ endpoint for the sequence topic.",
    note: "Only the first message frame is read, so Bitcoin Core's per-topic counter in the following frame is discarded and message loss cannot be detected.",
  },
  {
    key: "MYSQL_HOST",
    label: "Required.",
    meta: "database",
    tags: ["database", "required"],
    body: "Database host. Compose overrides it to the service name mysql.",
  },
  {
    key: "MYSQL_PORT",
    label: "Optional. Defaults to 3306.",
    meta: "database",
    tags: ["database"],
    body: "Database port.",
  },
  {
    key: "MYSQL_USER",
    label: "Required.",
    meta: "database",
    tags: ["database", "required"],
    body: "Database user. It needs data access plus the DDL rights to run migrations, unless you run migrations separately as a different user.",
  },
  {
    key: "MYSQL_PASSWORD",
    label: "Required.",
    meta: "database",
    tags: ["database", "required", "secret"],
    body: "Database password.",
  },
  {
    key: "MYSQL_DATABASE",
    label: "Required.",
    meta: "database",
    tags: ["database", "required"],
    body: "Database name. Use an empty, dedicated schema. The connection uses the utf8mb4_bin collation and the UTC timezone, and reads big numbers as strings.",
  },
  {
    key: "READINESS_MAX_BLOCK_LAG",
    label: "Optional. Defaults to 2.",
    meta: "readiness",
    tags: ["readiness"],
    body: "How many blocks the canonical tip may trail the node height before readiness reports canonical_tip_stale.",
  },
  {
    key: "AGREEMENT_KEY_ID",
    label: "Optional, but required as soon as any key is set.",
    meta: "signing",
    tags: ["signing", "operator"],
    body: "The identifier that travels in the signed envelope so a verifier knows which trusted public key to use. Limited to letters, digits, dot, underscore, colon and hyphen, up to 128 characters.",
    note: "Setting a private or public key without a key id fails the boot.",
  },
  {
    key: "AGREEMENT_PRIVATE_KEY_HEX",
    label: "Optional. 32-byte lowercase hex.",
    meta: "signing",
    tags: ["signing", "operator", "secret"],
    body: "The Ed25519 signing key for this pipeline's agreement tuples. Without it the readiness probe reports agreement_signer_unavailable and every verified route stays closed.",
    note: "Read as raw hex from the environment. There is no file, HSM, or KMS loader, so custody is entirely your design.",
  },
  {
    key: "AGREEMENT_PUBLIC_KEY_HEX",
    label: "Optional. 32-byte lowercase hex.",
    meta: "signing",
    tags: ["signing", "operator"],
    body: "An optional assertion of the public key. If present it must equal the key derived from the private key, otherwise the signing boundary is treated as unconfigured.",
    note: "Useful as a deployment guard against pasting the wrong secret.",
  },
  {
    key: "TANDEM_PARSER_COMMIT",
    label: "Optional. 20-byte lowercase hex, so 40 characters.",
    meta: "signing",
    tags: ["signing", "operator", "release"],
    body: "The commit of the parser artifact this pipeline is running. Part of the signed release identity.",
    note: "All four release identity values must be present before this pipeline can sign anything.",
  },
  {
    key: "TANDEM_INDEXER_COMMIT",
    label: "Optional. 20-byte lowercase hex.",
    meta: "signing",
    tags: ["signing", "operator", "release"],
    body: "The commit of the indexer artifact this pipeline is running.",
  },
  {
    key: "TANDEM_PARSER_BINARY_SHA256",
    label: "Optional. 32-byte lowercase hex.",
    meta: "signing",
    tags: ["signing", "operator", "release"],
    body: "The digest of the parser artifact. Signed evidence of exactly which build produced the numbers.",
  },
  {
    key: "TANDEM_INDEXER_BINARY_SHA256",
    label: "Optional. 32-byte lowercase hex.",
    meta: "signing",
    tags: ["signing", "operator", "release"],
    body: "The digest of the indexer artifact.",
    note: "Nothing in the repository recomputes these against a real artifact, so their accuracy is an operator commitment.",
  },
  {
    key: "PIPELINE_B_BASE_URL",
    label: "Optional. Absolute http or https URL.",
    meta: "verification",
    tags: ["verification", "operator", "external"],
    body: "Where the independently operated second pipeline lives. This pipeline requests its agreement at that base URL followed by /agreement/{height}. Trailing slashes are stripped.",
    note: "Rejected if it carries credentials, a query string, or a fragment. Unset means the verified surface returns 503 for everything.",
  },
  {
    key: "PIPELINE_B_REQUEST_TIMEOUT_MS",
    label: "Optional. Defaults to 5000.",
    meta: "verification",
    tags: ["verification"],
    body: "How long to wait for the second opinion before treating it as unavailable. A slow pipeline B closes the surface just as firmly as a wrong one.",
  },
  {
    key: "PIPELINE_A_TRUSTED_KEYS_JSON",
    label: "Optional. JSON object, defaults to empty.",
    meta: "verification",
    tags: ["verification", "operator"],
    body: "Maps each accepted key id to a 64 character lowercase hex Ed25519 public key for this pipeline. An envelope whose key id is absent is rejected before its signature is even checked.",
    note: "Parsed once at boot and frozen, so rotating a key needs a restart.",
  },
  {
    key: "PIPELINE_B_TRUSTED_KEYS_JSON",
    label: "Optional. JSON object, defaults to empty.",
    meta: "verification",
    tags: ["verification", "operator"],
    body: "The same map for the second pipeline.",
    note: "Nothing checks that the two maps are disjoint. Confirm yourself that no single signer can satisfy both sides of the comparison, because that would quietly turn two opinions into one.",
  },
  {
    key: "TANDEM_VERIFIED_MAINNET_ENABLED",
    label: "Optional. Defaults to false.",
    meta: "verification",
    tags: ["verification", "mainnet"],
    body: "When the network is mainnet and this is not exactly true, the gateway refuses before it asks this pipeline to sign anything. The check lives inside the gateway, so it cannot be bypassed by routing.",
    note: "Only the strings true and false are accepted. Anything else fails the boot.",
  },
  {
    key: "MYSQL_ROOT_PASSWORD",
    label: "Compose only. Not read by the application.",
    meta: "compose",
    tags: ["compose", "database", "secret"],
    body: "Used by the MySQL container to initialise the instance and by its health check. The indexer never reads it.",
  },
];
